{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "NODE_ENV=development nodemon src/index.js",
    "test": "NODE_ENV=development jest --watch --runInBand"
  },
  "jest": {
    "testEnvironment": "node",
    "testPathIgnorePatterns": [
      "/node_modules/",
      "/helper/"
    ]
  },
  "license": "ISC",
  "dependencies": {
    "bcrypt-nodejs": "*",
    "body-parser": "*",
    "cors": "^2.8.4",
    "express": "*",
    "jwt-simple": "*",
    "mongoose": "*",
    "morgan": "*",
    "nodemon": "*",
    "passport": "*",
    "passport-jwt": "*",
    "passport-local": "*",
    "validator": "^10.11.0"
  },
  "devDependencies": {
    "jest": "^24.5.0",
    "supertest": "^4.0.2"
  }
}
